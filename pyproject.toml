[project]
name = "facts-parent-partner"
version = "1.0.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "playwright>=1.62.0",
    "loguru>=0.7.3",
    "pyyaml>=6.0.3",
]

[project.scripts]
facts-parent-partner = "facts_parent_partner.cli:main"

[build-system]
requires = ["uv_build>=0.11.0,<0.12.0"]
build-backend = "uv_build"
